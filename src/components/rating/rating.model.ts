export interface RatingSettings {
    starsCount?: number;
    iconClass?: string;
    showRate?: boolean;
    showRateLabel?: boolean;
    levels?: Array<string>;
    size?: string;
}

export interface RatingLangs {
    rateLabel?: string;
}